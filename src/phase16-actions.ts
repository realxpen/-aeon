export const emitMissionAction=(name:string,detail?:unknown)=>{try{const A=window.AudioContext||((window as any).webkitAudioContext);if(A){const c=new A(),o=c.createOscillator(),g=c.createGain();o.frequency.value=620;g.gain.setValueAtTime(.0001,c.currentTime);g.gain.exponentialRampToValueAtTime(.02,c.currentTime+.008);g.gain.exponentialRampToValueAtTime(.0001,c.currentTime+.08);o.connect(g);g.connect(c.destination);o.start();o.stop(c.currentTime+.09)}}catch{};window.dispatchEvent(new CustomEvent(name,{detail}))}
export const missionActions={review:()=>emitMissionAction('aeon:review-products'),inspect:(basket?:unknown)=>emitMissionAction('aeon:inspect-basket',basket),approve:(basket?:unknown)=>emitMissionAction('aeon:approve-mission',basket),remove:(productId:string)=>emitMissionAction('aeon:remove-basket-item',{productId})}