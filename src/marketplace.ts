import { getMissionConstitution, MissionConstitution } from './mission-state'
export type Product={id:string;name:string;category:string;price:number;originalPrice:number;merchant:string;rating:number;performance:number;audio:number;stock:number;negotiable:boolean}
export const catalog:Product[]=[
{id:'creator-kit-01',name:'Creator Studio Kit',category:'Creator Kit',price:749000,originalPrice:799000,merchant:'AEON Marketplace',rating:4.9,performance:96,audio:94,stock:8,negotiable:true},
{id:'creator-kit-02',name:'Pro Creator Stack',category:'Creator Kit',price:895000,originalPrice:950000,merchant:'AEON Marketplace',rating:4.8,performance:98,audio:91,stock:5,negotiable:true},
{id:'creator-kit-03',name:'Mobile Creator Kit',category:'Creator Kit',price:529000,originalPrice:559000,merchant:'AEON Marketplace',rating:4.7,performance:88,audio:89,stock:14,negotiable:false},
{id:'creator-kit-04',name:'Audio-first Creator Rig',category:'Creator Kit',price:679000,originalPrice:720000,merchant:'AEON Marketplace',rating:4.9,performance:90,audio:99,stock:4,negotiable:true},
{id:'camera-01',name:'AEON Creator Camera',category:'Camera',price:315000,originalPrice:339000,merchant:'AEON Marketplace',rating:4.8,performance:96,audio:82,stock:9,negotiable:true},
{id:'mic-01',name:'Studio USB Microphone',category:'Microphone',price:85000,originalPrice:99000,merchant:'AEON Marketplace',rating:4.8,performance:86,audio:98,stock:16,negotiable:true},
{id:'light-01',name:'Creator LED Light',category:'Lighting',price:65000,originalPrice:75000,merchant:'AEON Marketplace',rating:4.7,performance:82,audio:80,stock:20,negotiable:false},
{id:'tripod-01',name:'Pro Creator Tripod',category:'Support',price:55000,originalPrice:65000,merchant:'AEON Marketplace',rating:4.7,performance:84,audio:80,stock:18,negotiable:false},
{id:'phone-01',name:'AEON Pro Phone 256GB',category:'Phone',price:399000,originalPrice:429000,merchant:'AEON Marketplace',rating:4.8,performance:94,audio:91,stock:12,negotiable:true},
{id:'phone-02',name:'AEON Creator Phone Max',category:'Phone',price:489000,originalPrice:529000,merchant:'AEON Marketplace',rating:4.9,performance:97,audio:94,stock:7,negotiable:true},
{id:'phone-03',name:'AEON Lite Phone',category:'Phone',price:229000,originalPrice:249000,merchant:'AEON Marketplace',rating:4.5,performance:82,audio:84,stock:20,negotiable:false}
]
const aliases:Record<string,string>={smartphone:'Phone',iphone:'Phone',mobile:'Phone',phone:'Phone',camera:'Camera',microphone:'Microphone',mic:'Microphone',lighting:'Lighting',light:'Lighting',tripod:'Support',support:'Support','creator kit':'Creator Kit'}
const stopWords=new Set(['i','need','want','a','an','the','me','my','for','to','please','find','get','buy','looking','look','can','you','could','would','and','under','below','less','than','with','best','deal','price','good','some','of'])
const normalized=(q:string)=>q.trim().toLowerCase().replace(/[^a-z0-9₦\s]/g,' ')
const meaningfulTokens=(q:string)=>normalized(q).split(/\s+/).filter(t=>t&&!stopWords.has(t)&&!/^\d+$/.test(t)&&t!=='₦')

/** Classifies input before any marketplace search. Vague intent is clarified, never executed. */
export function classifyMissionInput(goal:string):'valid'|'ambiguous'|'invalid'{
 const raw=goal.trim()
 if(!raw||raw.length>2000)return 'invalid'
 const q=normalized(raw)
 if(/\b(surprise me|surprise|something nice|anything|whatever|you choose|pick something|choose for me)\b/i.test(q))return 'ambiguous'
 const tokens=meaningfulTokens(q)
 if(tokens.length<1)return 'invalid'
 if(Object.keys(aliases).some(k=>q.includes(k)))return 'valid'
 const productTerms=['laptop','computer','pc','tablet','headphone','headphones','earbuds','monitor','keyboard','mouse','console','tv','television','watch','printer','router','speaker','desk','chair','backpack','shoes','camera','microphone','tripod','light','phone','smartphone']
 if(productTerms.some(term=>q.includes(term)))return 'valid'
 const commerceVerb=/\b(find|search|get|buy|purchase|shop|shopping|order|compare|negotiate|looking for|recommend)\b/i.test(raw)
 const commerceConstraint=/₦|\b(under|below|budget|maximum|max|ceiling|within|affordable|cheapest|lowest price)\b/i.test(raw)
 if(!commerceVerb||!commerceConstraint)return 'invalid'
 const hasWordLikeToken=tokens.some(t=>/[aeiou]/.test(t)&&t.length>=3)
 const repeated=tokens.filter(t=>/(.)\1\1/.test(t)).length
 return hasWordLikeToken&&repeated<2?'valid':'invalid'
}

export function searchCatalog(query:string,maxPrice?:number){const q=normalized(query);const exact=aliases[q]??(aliases[Object.keys(aliases).find(k=>q.includes(k))??'']??'');if(exact)return catalog.filter(p=>p.category===exact&&(!maxPrice||p.price<=maxPrice)).sort((a,b)=>b.rating-a.rating);const tokens=meaningfulTokens(q);return catalog.filter(p=>(!maxPrice||p.price<=maxPrice)&&tokens.some(t=>[p.name,p.category,p.merchant].join(' ').toLowerCase().includes(t))).sort((a,b)=>b.rating-a.rating)}
export type MissionRequirement={category:string;label:string;required:true;products:Product[];cheapest?:Product}
export function searchMissionRequirements(goal:string,budget?:number):MissionRequirement[]{const q=normalized(goal);const categories:string[]=[];if(/creator|content|studio|audio|video|stream/i.test(q))categories.push('Camera','Microphone','Lighting','Support');if(/phone|smartphone|iphone|mobile/i.test(q))categories.push('Phone');if(/camera/i.test(q)&&!categories.includes('Camera'))categories.push('Camera');if(/microphone|mic/i.test(q)&&!categories.includes('Microphone'))categories.push('Microphone');if(/light|lighting/i.test(q)&&!categories.includes('Lighting'))categories.push('Lighting');if(/tripod/i.test(q)&&!categories.includes('Support'))categories.push('Support');if(!categories.length){const products=searchCatalog(goal,budget);return products.length?[{category:'SEARCH',label:'Marketplace matches',required:true,products,cheapest:products[products.length-1]}]:[]}return categories.map(category=>{const products=searchCatalog(category,budget);const all=searchCatalog(category);return {category,label:category==='Phone'?'Smartphone':category,required:true,products,cheapest:all[all.length-1]}}).filter(r=>r.products.length>0)}
export type NegotiationMessage={speaker:'AEON'|'SELLER AGENT'|'CONSTITUTION';text:string;price?:number;round:number}
export type NegotiationResult={acceptedPrice:number;saving:number;rounds:number;message:string;status:'approved'|'rejected';reason?:string;transcript:NegotiationMessage[]}
const money=(n:number)=>`₦${n.toLocaleString()}`
export type NegotiationIntent='improve'|'cheapest'|'required'|'none'
export function getNegotiationIntent(goal:string,product:Product,ceiling:number):NegotiationIntent{const q=normalized(goal);if(!product.negotiable)return 'none';if(/quick|quickly|fast|immediately|asap/i.test(q))return 'none';if(/cheapest|lowest price|as cheap as possible|best price|best deal|save as much|maximum savings/i.test(q))return 'cheapest';if(Number.isFinite(ceiling)&&product.price>ceiling)return 'required';return 'improve'}
export function negotiate(product:Product,budget:number,constitution:MissionConstitution=getMissionConstitution()):NegotiationResult{const base=product.price;const transcript:NegotiationMessage[]=[];const constitutionCeiling=Number(constitution.budget??0);const ceiling=Number.isFinite(budget)&&budget>0?Math.min(budget,constitutionCeiling>0?constitutionCeiling:Infinity):(constitutionCeiling>0?constitutionCeiling:Infinity);const intent=getNegotiationIntent(constitution.goal,product,ceiling);if(!constitution.canNegotiate||intent==='none'){if(!constitution.canNegotiate)transcript.push({speaker:'CONSTITUTION',round:0,text:'Negotiation is locked by the active constitution.'});else transcript.push({speaker:'CONSTITUTION',round:0,text:'Negotiation is not needed for this mission or this vendor.'});return {acceptedPrice:base,saving:0,rounds:0,status:'rejected',message:intent==='none'?'Listed deal retained.':'AEON blocked this negotiation.',reason:intent==='none'?'NEGOTIATION_NOT_NEEDED':'CONSTITUTION_NEGOTIATION_DISABLED',transcript}}if(base>ceiling&&intent!=='required'){transcript.push({speaker:'CONSTITUTION',round:0,text:`Listed price ${money(base)} exceeds the authorized ceiling of ${money(ceiling)}.`});return {acceptedPrice:base,saving:0,rounds:0,status:'rejected',message:'Deal exceeds the authorized budget.',reason:'CONSTITUTION_BUDGET_EXCEEDED',transcript}}const opener=intent==='cheapest'?`I found ${product.name} at ${money(base)}. My buyer wants the best possible price. What's your strongest offer?`:`I found ${product.name} listed at ${money(base)}. It fits the mission, but I'd like to improve the price. Can you do better?`;transcript.push({speaker:'AEON',round:1,text:opener,price:base});const vendorRate=intent==='cheapest'?0.94:0.97;const vendorCounter=Math.max(Math.round((base*vendorRate)/1000)*1000,Math.round((product.originalPrice*.90)/1000)*1000);transcript.push({speaker:'SELLER AGENT',round:1,text:`I can improve the offer from ${money(base)} to ${money(vendorCounter)}.`,price:vendorCounter});const target=Math.min(ceiling,Math.round((base*(intent==='cheapest'?0.88:0.94))/1000)*1000);if(target<vendorCounter){transcript.push({speaker:'AEON',round:2,text:`Can you do ${money(target)}? That would give my buyer materially better value.`,price:target});const vendorAccept=target>=Math.round(base*.91/1000)*1000;if(vendorAccept){transcript.push({speaker:'SELLER AGENT',round:2,text:`Agreed. ${money(target)} is my best final offer.`,price:target});transcript.push({speaker:'CONSTITUTION',round:2,text:`Final negotiated price ${money(target)} is within the ${money(ceiling)} authority ceiling.`,price:target});return {acceptedPrice:target,saving:base-target,rounds:2,status:'approved',message:'Vendor accepted AEON’s counter-offer.',transcript}}transcript.push({speaker:'SELLER AGENT',round:2,text:`I can't go that low. My final offer remains ${money(vendorCounter)}.`,price:vendorCounter})}const accepted=vendorCounter<=ceiling?vendorCounter:base;if(accepted<base){transcript.push({speaker:'CONSTITUTION',round:2,text:`Final vendor offer ${money(accepted)} is within the ${money(ceiling)} authority ceiling.`,price:accepted});return {acceptedPrice:accepted,saving:base-accepted,rounds:2,status:'approved',message:'Vendor counter-offer accepted as the best available deal.',transcript}}transcript.push({speaker:'CONSTITUTION',round:2,text:'No compliant negotiated price was reached.',price:base});return {acceptedPrice:base,saving:0,rounds:2,status:'rejected',message:'No compliant negotiated deal available.',reason:'NO_COMPLIANT_NEGOTIATED_PRICE',transcript}}
export function rankForMission(products:Product[],priorities:string[]){return [...products].map(p=>({...p,matchScore:Math.round(((priorities.includes('performance')?p.performance:80)*.4+(priorities.includes('audio quality')?p.audio:80)*.4+p.rating*20*.2))})).sort((a,b)=>b.matchScore-a.matchScore)}
