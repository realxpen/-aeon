import { searchCatalog, rankForMission, negotiate } from './marketplace'
import { getMissionConstitution } from './mission-state'
type Tool={name:string;description:string;inputSchema:Record<string,unknown>;execute:(input:any)=>Promise<unknown>}
const blocked=(reason:string,message:string)=>({status:'blocked',blockedBy:'AEON_CONSTITUTION',reason,message})
export const aeonTools:Tool[]=[
{name:'create_mission',description:'Create an AEON mission with a goal, budget and authority boundaries.',inputSchema:{type:'object',properties:{goal:{type:'string'},budget:{type:'number'},canNegotiate:{type:'boolean'},purchaseRequiresApproval:{type:'boolean'}},required:['goal','budget']},execute:async(input)=>({ok:true,missionId:'mission_demo_001',status:'created',constitution:{...input,purchaseRequiresApproval:input.purchaseRequiresApproval??true}})},
{name:'search_products',description:'Search the AEON product marketplace for candidates that satisfy the active mission.',inputSchema:{type:'object',properties:{query:{type:'string'},maxPrice:{type:'number'}},required:['query']},execute:async(input)=>{const c=getMissionConstitution();return {query:input.query,maxPrice:c.budget,results:searchCatalog(input.query,Math.min(input.maxPrice??c.budget,c.budget))}}},
{name:'compare_products',description:'Rank marketplace candidates against the active mission priorities.',inputSchema:{type:'object',properties:{productIds:{type:'array',items:{type:'string'}},priorities:{type:'array',items:{type:'string'}}},required:['productIds']},execute:async(input)=>{const c=getMissionConstitution();return {ranked:rankForMission(searchCatalog('creator',c.budget),c.priorities).filter(p=>input.productIds.includes(p.id)),explanation:'Ranking is governed by the active mission constitution.'}}},
{name:'negotiate_offer',description:'Request a marketplace negotiation. AEON constitution is checked before negotiation execution.',inputSchema:{type:'object',properties:{productId:{type:'string'},budget:{type:'number'}},required:['productId']},execute:async(input)=>{const c=getMissionConstitution();if(!c.canNegotiate)return blocked('CONSTITUTION_NEGOTIATION_DISABLED','Negotiation was refused before the negotiation engine executed because this mission does not authorize negotiation.');const p=searchCatalog('creator').find(x=>x.id===input.productId);if(!p)return {status:'error',reason:'PRODUCT_NOT_FOUND'};const requestedBudget=Number(input.budget??c.budget);if(p.price>c.budget||requestedBudget>c.budget)return blocked('CONSTITUTION_BUDGET_EXCEEDED',`Offer blocked before negotiation: mission ceiling is ₦${c.budget.toLocaleString()}.`);return negotiate(p,Math.min(requestedBudget,c.budget),c)}},
{name:'request_purchase_approval',description:'Stop before purchase and request an explicit human decision.',inputSchema:{type:'object',properties:{productId:{type:'string'},price:{type:'number'},reason:{type:'string'}},required:['productId','price']},execute:async(input)=>{const c=getMissionConstitution();if(Number(input.price)>c.budget)return blocked('CONSTITUTION_BUDGET_EXCEEDED','Purchase approval cannot be requested for a price above the mission budget.');return {status:'approval_required',humanDecision:true,action:'purchase',...input}}}
]
export function registerWebMCP(){const mc=(navigator as any).modelContext;if(!mc?.registerTool)return false;for(const tool of aeonTools)mc.registerTool(tool);return true}
