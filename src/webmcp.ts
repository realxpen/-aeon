type Tool={name:string;description:string;inputSchema:Record<string,unknown>;execute:(input:any)=>Promise<unknown>}
export const aeonTools:Tool[]=[
{name:'create_mission',description:'Create an AEON mission with a goal, budget and authority boundaries.',inputSchema:{type:'object',properties:{goal:{type:'string'},budget:{type:'number'},canNegotiate:{type:'boolean'},purchaseRequiresApproval:{type:'boolean'}},required:['goal','budget']},execute:async(input)=>({ok:true,missionId:'mission_demo_001',status:'created',constitution:{...input,purchaseRequiresApproval:input.purchaseRequiresApproval??true}})},
{name:'search_products',description:'Search the connected product catalog for candidates that satisfy an AEON mission.',inputSchema:{type:'object',properties:{query:{type:'string'},maxPrice:{type:'number'}},required:['query']},execute:async(input)=>({query:input.query,maxPrice:input.maxPrice??null,results:[{id:'creator-kit-01',name:'Creator Studio Kit',price:749000,matchScore:.94,reasons:['within budget','strong performance','high audio quality']},{id:'creator-kit-02',name:'Pro Creator Stack',price:895000,matchScore:.91,reasons:['within budget','premium audio','high performance']}]} )},
{name:'compare_products',description:'Compare candidate products against the mission priorities and return a ranked recommendation.',inputSchema:{type:'object',properties:{productIds:{type:'array',items:{type:'string'}},priorities:{type:'array',items:{type:'string'}}},required:['productIds']},execute:async(input)=>({ranked:[{id:'creator-kit-01',score:.94,decision:'recommended'},{id:'creator-kit-02',score:.91,decision:'alternative'}],explanation:'Creator Studio Kit best matches the stated performance and audio priorities while preserving budget headroom.'})},
{name:'request_purchase_approval',description:'Stop before purchase and request an explicit human decision.',inputSchema:{type:'object',properties:{productId:{type:'string'},price:{type:'number'},reason:{type:'string'}},required:['productId','price']},execute:async(input)=>({status:'approval_required',humanDecision:true,action:'purchase',...input})}
]
export function registerWebMCP(){const mc=(navigator as any).modelContext;if(!mc?.registerTool)return false;for(const tool of aeonTools)mc.registerTool(tool);return true}
