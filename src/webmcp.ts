import { searchCatalog, rankForMission, negotiate } from './marketplace'
type Tool={name:string;description:string;inputSchema:Record<string,unknown>;execute:(input:any)=>Promise<unknown>}
export const aeonTools:Tool[]=[
{name:'create_mission',description:'Create an AEON mission with a goal, budget and authority boundaries.',inputSchema:{type:'object',properties:{goal:{type:'string'},budget:{type:'number'},canNegotiate:{type:'boolean'},purchaseRequiresApproval:{type:'boolean'}},required:['goal','budget']},execute:async(input)=>({ok:true,missionId:'mission_demo_001',status:'created',constitution:{...input,purchaseRequiresApproval:input.purchaseRequiresApproval??true}})},
{name:'search_products',description:'Search the AEON product marketplace for candidates that satisfy a mission.',inputSchema:{type:'object',properties:{query:{type:'string'},maxPrice:{type:'number'}},required:['query']},execute:async(input)=>({query:input.query,maxPrice:input.maxPrice??null,results:searchCatalog(input.query,input.maxPrice)})},
{name:'compare_products',description:'Rank marketplace candidates against mission priorities.',inputSchema:{type:'object',properties:{productIds:{type:'array',items:{type:'string'}},priorities:{type:'array',items:{type:'string'}}},required:['productIds']},execute:async(input)=>({ranked:rankForMission(searchCatalog('creator'),input.priorities).filter(p=>input.productIds.includes(p.id)),explanation:'Ranking balances mission priorities, rating and budget.'})},
{name:'negotiate_offer',description:'Negotiate a marketplace offer within the agent budget and authority limits.',inputSchema:{type:'object',properties:{productId:{type:'string'},budget:{type:'number'}},required:['productId','budget']},execute:async(input)=>{const p=searchCatalog('creator').find(x=>x.id===input.productId);return p?negotiate(p,input.budget):{error:'product_not_found'}}},
{name:'request_purchase_approval',description:'Stop before purchase and request an explicit human decision.',inputSchema:{type:'object',properties:{productId:{type:'string'},price:{type:'number'},reason:{type:'string'}},required:['productId','price']},execute:async(input)=>({status:'approval_required',humanDecision:true,action:'purchase',...input})}
]
export function registerWebMCP(){const mc=(navigator as any).modelContext;if(!mc?.registerTool)return false;for(const tool of aeonTools)mc.registerTool(tool);return true}
